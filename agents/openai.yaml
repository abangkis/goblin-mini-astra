interface:
  display_name: "Goblin Mini Astra"
  short_description: "GPT-6 Sol coordination with Luna and Astra"
  default_prompt: "Use $goblin-mini-astra to complete this task. Start or reuse a native goal with my default or explicit token budget without another confirmation; for no budget, do not create a native goal or set a token budget."
policy:
  allow_implicit_invocation: true
